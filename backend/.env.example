# MySQL (Prisma) - bate com o docker-compose.yml
# root é usado para permitir que o Prisma crie o shadow database das migrations
DATABASE_URL="mysql://root:rootpass@localhost:3306/techx"

# MongoDB (Mongoose)
MONGO_URL="mongodb://localhost:27017/techx"

# JWT
JWT_SECRET="troque-este-segredo-em-producao"
JWT_EXPIRES_IN="7d"

# Servidor
PORT=3333
CORS_ORIGIN="http://localhost:4200"
